<!DOCTYPE html>
<html lang="tr" data-bs-theme="dark">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <meta name="description" content="{% block meta_description %}BAM (Borsa Analiz Motoru) ile Borsa İstanbul (BIST) hisselerini yapay zeka ve algoritmik sistemlerle analiz edin, fırsatları yakalayın.{% endblock %}">
    <meta name="robots" content="{% block robots %}index, follow{% endblock %}">
    <link rel="canonical" href="https://www.bamterminal.com.tr{{ app.request.pathInfo }}">
    <meta property="og:title" content="{% block og_title %}{{ block('title') }}{% endblock %}">
    <meta property="og:description" content="{{ block('meta_description') }}">
    <meta property="og:url" content="https://www.bamterminal.com.tr{{ app.request.pathInfo }}">
    <meta property="og:type" content="website">
    <meta property="og:image" content="https://www.bamterminal.com.tr/images/og-image.png">
    
    <meta name="twitter:card" content="summary_large_image">
    <meta name="twitter:title" content="{{ block('title') }}">
    <meta name="twitter:description" content="{{ block('meta_description') }}">
    <meta name="twitter:image" content="https://www.bamterminal.com.tr/images/og-image.png">
    
    <title>{% block title %}BAM - Borsa Analiz Motoru{% endblock %}</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.11.3/font/bootstrap-icons.min.css">
    <link rel="icon" style="border-radius: 20px" type="image/png" href="{{ asset('images/logo/bam-logo.ico') }}?v=1">
    <style>
        body {
            font-family: Inter, system-ui, -apple-system, BlinkMacSystemFont, "Segoe UI", sans-serif;
            background-color: #121826;
            color: #f3f4f6;
            display: flex;
            flex-direction: column;
            min-height: 100vh;
        }

        main { flex: 1; }

        .card-custom {
            background-color: #1f2937;
            border: 1px solid #374151;
            border-radius: 1rem;
        }

        .text-yellow-custom { color: #eab308; }

        .btn-yellow-custom {
            background-color: #ca8a04;
            color: white;
            border: none;
            transition: all 0.2s ease;
        }

        .btn-yellow-custom:hover {
            background-color: #a16207;
            color: white;
            transform: translateY(-1px);
        }

        .bg-green-soft {
            background-color: rgba(22, 101, 52, 0.3);
            border-color: #166534;
        }

        .bg-red-soft {
            background-color: rgba(153, 27, 27, 0.3);
            border-color: #991b1b;
        }

        .tracking-widest { letter-spacing: 0.08em; }

        .ai-summary-box {
            background-color: #111827;
            border: 1px solid #1f2937;
            border-radius: 0.75rem;
            padding: 1.25rem;
            color: #d1d5db;
        }

        .custom-badge-hover:hover {
            background-color: #ca8a04 !important;
            color: #121826 !important;
        }
        
        /* Footer Hover Styles */
        .hover-warning:hover {
            color: #f59e0b !important;
        }
    </style>
    <link rel="stylesheet" href="{{ asset('css/bam-personal.css') }}?v=2">
</head>
<body class="bam-page">

{% include 'partials/_dashboard_header.html.twig' %}

<main class="py-4 py-lg-5">
    {% block content %}{% endblock %}
</main>

<footer class="mt-auto border-top" style="background-color: #0f1520; border-color: rgba(255,255,255,0.05) !important;">
    <div class="container py-5">
        <div class="row g-4">
            <!-- Marka ve Aciklama -->
            <div class="col-lg-4 col-md-6">
                <a class="d-flex align-items-center text-decoration-none mb-3" href="{{ path('app-home') }}">
                    <img src="{{ asset('images/logo/bam-images.png') }}" alt="BAM Logo" style="width:36px; height:36px; object-fit:contain;" class="me-2">
                    <span class="text-light fw-bold fs-4 tracking-widest">BAM</span>
                </a>
                <p class="text-secondary opacity-75 small lh-lg pe-lg-4">
                    Borsa İstanbul yatırımcıları için yapay zeka destekli, gerçek zamanlı ve profesyonel finansal analiz terminali.
                </p>
                <div class="d-flex gap-3 mt-4">
{#                    <a href="https://twitter.com/bamterminal" target="_blank" class="text-secondary hover-warning transition-all"><i class="bi bi-twitter-x fs-5"></i></a>#}
                    <a href="https://www.linkedin.com/in/muhammed-g%C3%BCrkan-altunba%C5%9F-992ab0347/" target="_blank" class="text-secondary hover-warning transition-all"><i class="bi bi-linkedin fs-5"></i></a>
                    <a href="mailto:muhammedgurkanaltunbas@gmail.com" class="text-secondary hover-warning transition-all"><i class="bi bi-envelope fs-5"></i></a>
                </div>
            </div>

            <!-- Kurumsal -->
            <div class="col-lg-2 col-md-6">
                <h6 class="text-light fw-bold text-uppercase tracking-widest mb-4">Kurumsal</h6>
                <ul class="list-unstyled mb-0 d-flex flex-column gap-2">
                    <li><a href="{{ path('app_about') }}" class="text-secondary text-decoration-none hover-warning transition-all small">Hakkımızda</a></li>
                    <li><a href="{{ path('app_blog') }}" class="text-secondary text-decoration-none hover-warning transition-all small">Blog</a></li>
                    <li><a href="{{ path('app_contact') }}" class="text-secondary text-decoration-none hover-warning transition-all small">İletişim</a></li>
                    <li><a href="{{ path('app_legal') }}" class="text-secondary text-decoration-none hover-warning transition-all small">Yasal Uyarı</a></li>
                </ul>
            </div>

            <!-- Platform -->
            <div class="col-lg-2 col-md-6">
                <h6 class="text-light fw-bold text-uppercase tracking-widest mb-4">Platform</h6>
                <ul class="list-unstyled mb-0 d-flex flex-column gap-2">
                    <li><a href="{{ path('app-home') }}" class="text-secondary text-decoration-none hover-warning transition-all small">Piyasa Ekranı</a></li>
                    <li><a href="{{ path('app_watchlist') }}" class="text-secondary text-decoration-none hover-warning transition-all small">Takip Listesi</a></li>
                    <li><a href="{{ path('app_portfolio') }}" class="text-secondary text-decoration-none hover-warning transition-all small">Portföy Yönetimi</a></li>
                    <li><a href="{{ path('app_ai_reports') }}" class="text-secondary text-decoration-none hover-warning transition-all small">Yapay Zeka Raporları</a></li>
                </ul>
            </div>

            <!-- Iletisim & Destek -->
            <div class="col-lg-4 col-md-6">
                <h6 class="text-light fw-bold text-uppercase tracking-widest mb-4">Destek & İletişim</h6>
                <ul class="list-unstyled mb-0 d-flex flex-column gap-3">
                    <li class="d-flex align-items-start gap-3">
                        <i class="bi bi-geo-alt text-warning mt-1"></i>
                        <span class="text-secondary small">Çorum, Türkiye</span>
                    </li>
                    <li class="d-flex align-items-center gap-3">
                        <i class="bi bi-envelope-paper text-warning"></i>
                        <a href="mailto:info@bamterminal.com.tr" class="text-secondary text-decoration-none hover-warning small">muhammedgurkanaltunbas@gmail.com</a>
                    </li>
                </ul>
            </div>
        </div>

        <hr class="my-5 border-secondary opacity-25">

        <!-- Alt Telif & Yasal Uyari -->
        <div class="row align-items-center">
            <div class="col-lg-5 text-center text-lg-start mb-3 mb-lg-0">
                <p class="text-secondary small mb-0 tracking-widest">&copy; {{ 'now'|date('Y') }} BAM Terminal. Tüm hakları saklıdır.</p>
            </div>
            <div class="col-lg-7 text-center text-lg-end">
                <p class="text-secondary opacity-75 mb-0" style="font-size: 0.75rem; line-height: 1.6;">
                    <strong>SPK Uyarısı:</strong> Burada yer alan yorum ve tavsiyeler yatırım danışmanlığı kapsamında değildir. 
                    Verilen kararların tüm riski kullanıcıya aittir. Detaylar için <a href="{{ path('app_legal') }}" class="text-warning text-decoration-none">Yasal Uyarı</a> metnimizi okuyun.
                </p>
            </div>
        </div>
    </div>
</footer>

<script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>
<script>
    document.addEventListener('DOMContentLoaded', function() {
        sonArananlariGoster();
    });

    let symbol = "{{ symbol|default('') }}";

    if (symbol && symbol.trim() !== '') {
        symbol = symbol.toUpperCase().trim();

        let arananlar = JSON.parse(localStorage.getItem('arananSymbolList')) || [];

        if (!arananlar.includes(symbol)) {
            arananlar.unshift(symbol);
            if (arananlar.length > 10) {
                arananlar.pop();
            }
            localStorage.setItem('arananSymbolList', JSON.stringify(arananlar));
        }
    }

    function sonArananlariGoster() {
        let arananlar = JSON.parse(localStorage.getItem('arananSymbolList')) || [];
        let listeHtml = document.getElementById('sonArananlarList');

        if (!listeHtml) {
            return;
        }

        if (arananlar.length === 0) {
            listeHtml.innerHTML = '<span class="text-secondary small opacity-50">Henüz aranan hisse yok</span>';
            return;
        }

        listeHtml.innerHTML = arananlar.map(symbol =>
            `<a href="?symbol=${symbol}" class="badge bg-dark border border-secondary text-secondary text-decoration-none px-3 py-2 rounded-pill custom-badge-hover transition-all">
                ${symbol}
            </a>`
        ).join('');
    }

    function temizleSonArananlar() {
        localStorage.removeItem('arananSymbolList');
        sonArananlariGoster();
    }

    function localStorageTemizle() {
        if (confirm('Tüm arama geçmişinizi temizlemek istediğinize emin misiniz?')) {
            localStorage.clear();
            sonArananlariGoster();
        }
    }

    function toggleNewsList() {
        const container = document.getElementById('newsListContainer');
        const arrow = document.getElementById('arrowIcon');

        if (!container || !arrow) {
            return;
        }

        if (container.classList.contains('d-none')) {
            container.classList.remove('d-none');
            arrow.classList.replace('bi-chevron-down', 'bi-chevron-up');
        } else {
            container.classList.add('d-none');
            arrow.classList.replace('bi-chevron-up', 'bi-chevron-down');
        }
    }
</script>
</body>
</html>
