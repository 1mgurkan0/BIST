{% extends 'admin/base.html.twig' %}

{% block title %}Panel{% endblock %}
{% block page_title %}Genel Bakış{% endblock %}

{% block content %}
    <div class="row g-4">
        <div class="col-md-4">
            <div class="admin-card p-4">
                <div class="text-secondary small mb-1">Toplam Yazı</div>
                <div class="fs-3 fw-bold text-light">{{ totalPosts }}</div>
            </div>
        </div>
        <div class="col-md-4">
            <div class="admin-card p-4">
                <div class="text-secondary small mb-1">Taslaklar</div>
                <div class="fs-3 fw-bold text-light">{{ draftPosts }}</div>
            </div>
        </div>
        <div class="col-md-4">
            <div class="admin-card p-4">
                <div class="text-secondary small mb-1">Medya Dosyası</div>
                <div class="fs-3 fw-bold text-light">{{ totalMedia }}</div>
            </div>
        </div>
    </div>
{% endblock %}
